{
  "name": "electron-alarm-clock",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ./main.js",
    "build": "electron-packager ./main.js app --win --out presenterTool --arch=x64 --overwrite --ignore=node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^18.0.3",
    "timer.js": "^1.0.4"
  },
  "devDependencies": {
    "electron-packager": "^15.4.0"
  }
}
